\doxysection{httpx.\+\_\+client.\+Use\+Client\+Default Class Reference}
\hypertarget{classhttpx_1_1__client_1_1UseClientDefault}{}\label{classhttpx_1_1__client_1_1UseClientDefault}\index{httpx.\_client.UseClientDefault@{httpx.\_client.UseClientDefault}}


\doxysubsection{Detailed Description}
\begin{DoxyVerb}For some parameters such as `auth=...` and `timeout=...` we need to be able
to indicate the default "unset" state, in a way that is distinctly different
to using `None`.

The default "unset" state indicates that whatever default is set on the
client should be used. This is different to setting `None`, which
explicitly disables the parameter, possibly overriding a client default.

For example we use `timeout=USE_CLIENT_DEFAULT` in the `request()` signature.
Omitting the `timeout` parameter will send a request using whatever default
timeout has been configured on the client. Including `timeout=None` will
ensure no timeout is used.

Note that user code shouldn't need to use the `USE_CLIENT_DEFAULT` constant,
but it is used internally when a parameter is not included.
\end{DoxyVerb}
 

The documentation for this class was generated from the following file:\+\begin{DoxyCompactItemize}
\item 
venv/\+lib/\+python3.\+12/\+site-\/packages/\+httpx/\+\+\_\+client.\+py\end{DoxyCompactItemize}
